# Postgres props
user=brbl_admin
password=brbl_admin
ssl=false

# Connection pool (c3p0) config
driverClass=org.postgresql.Driver
jdbcUrl=jdbc:postgresql://localhost:5432/brbl_db_dev
initialPoolSize=1
minPoolSize=1
maxPoolSize=1
acquireIncrement=1
