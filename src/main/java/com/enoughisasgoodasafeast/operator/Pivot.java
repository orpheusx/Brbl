package com.enoughisasgoodasafeast.operator;

import com.enoughisasgoodasafeast.MOMessage;
import com.enoughisasgoodasafeast.MTMessage;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.io.IOException;

public class Pivot {

    private static final Logger LOG = LoggerFactory.getLogger(Pivot.class);

    public static Script evaluate(Session session) throws IOException {
//        String mtText = renderForPlatform(moMessage.platform(), session.currentScript().text());
//        MTMessage mt = MTMessage.create(moMessage, mtText);
//        session.addOutput(mt);
        return null;
    }
}
